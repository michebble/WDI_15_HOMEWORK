all games listed here
<% if !@games.nil? %>
  <% @games.each do |game|%>
    <div>
      <a href= "/games/<%= game.id %>" ><img src="<%= game.image_url %>"></a>
    </div>
    <div>
    <%= game.title %> 
    </div>
  <% end %>
<% end %>